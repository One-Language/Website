{
  "private": true,
  "name": "Max",
  "version": "1.0.0",
  "description": "All things One and Max Base",
  "main": "index.js",
  "scripts": {
    "build": "wrangler build",
    "publish": "wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "BaseMax",
  "devDependencies": {
    "prettier": "2.8.0"
  }
}
